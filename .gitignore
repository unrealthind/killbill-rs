# --- Local development / editor working files --------------------------
# Not tracked: contributor-facing docs live in CONTRIBUTING.md and
# docs/DESIGN-NOTES.md; PROJECT_CHARTER.md is the tracked source of truth.
.claude/
.claude-*/
CLAUDE.local.md
CLAUDE.*.md
AGENTS.md
working.md
# Local review / audit write-ups are working notes, not project history. What
# they conclude belongs in the charter's or the changelog's record.
AUDIT-*.md
audit-*.md
.aider*
.cursor/
.mcp.json
*.claude.md

# --- Release signing keys -------------------------------------------
# The minisign secret key is the release trust root and must never enter the
# repo or this machine's history. The public key lives in SECURITY.md.
*.key
killbill-rs.pub
minisign.key

# --- Local test artifacts --------------------------------------------
# Throwaway daemon configs for hardware testing, never installed.
/test-config.toml
/test-config-live.toml

# --- Rust -----------------------------------------------------------
/target/
**/target/
Cargo.lock.orig
*.rs.bk
*.pdb

# --- Packaging build output --------------------------------------
*.deb
*.rpm
*.tar.gz
*.tar.zst
*.pkg.tar.*
/SHA256SUMS
*.minisig
/dist/
packaging/aur/*.tar.gz
packaging/aur/pkg/
packaging/aur/src/

# --- Editor / OS -------------------------------------------------------
.DS_Store
*.swp
*.swo
.idea/
.vscode/
